<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CRStats" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CRStats" parent="Base.Theme.CRStats" />
    <style name="CustomSearchHintStyle" parent="TextAppearance.AppCompat">
        <item name="android:textColorHint">@color/blue</item>
    </style>
</resources>